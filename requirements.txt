# ── Phase 1: Core (required) ──────────────────────────────────────────────────
httpx[http2]          # Async HTTP client with HTTP/2 support
beautifulsoup4        # HTML parser
lxml                  # Fast HTML parser backend for BeautifulSoup
langdetect            # Language identification from text
anthropic             # Official Anthropic / Claude API SDK
pydantic>=2.0         # Data models and validation
pandas                # CSV loading and data manipulation
tqdm                  # Progress bars
python-dotenv         # Load .env files

# ── Phase 2: Power features ────────────────────────────────────────────────────
# Install all at once:
#   pip install playwright dnspython gspread google-auth ddgs
#   python -m playwright install chromium

playwright            # Headless Chromium fallback for JS-heavy / bot-protected sites
dnspython             # Email DNS MX validation
gspread               # Google Sheets API client
google-auth           # Google service account authentication
ddgs                  # DuckDuckGo search for URL auto-discovery (free, no API key)
